// Add loading animations with the data storage bar
let storageBar = document.querySelector(".flyo > .storage > .progress div");
let storageBarWidth = (815 / 1000) * 100;

window.onload = function () {
  storageBar.style.width = `81.5%`;
};
